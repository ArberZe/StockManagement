@page

@inject StockManagement.App.Contracts.IAuthenticationService _authenticationService

@functions{
    public async Task<IActionResult> OnGet()
    {
        await _authenticationService.Logout();
        return Redirect("~/");
    }
}